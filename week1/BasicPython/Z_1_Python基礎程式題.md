
#
```
問題如下
有五個數字：1、2、3、4，6能組成多少個互不相同且無重複數字的四位元數？
總和是多少？
```

#
```
問題如下
輸入三個整數x,y,z，請把這三個數由小到大輸出。
```

#
```
反轉一個只有3位元數的整數。
輸入 number= 123 ，輸出 321 ；輸入 number= 900 ，輸出9
```
#
```
合併兩個昇冪的整數陣列 ，形成一個新的陣列，新陣列也要有序。

輸入 A=[l],B=[l] ，輸出 [1,1] 
輸入 A= [1,2,3,4],B =[2,4,5,6] ，輸出 [1,2,2,3,4,4,5,6] 
```
#
```
輸入一個5位數，撰寫程式判斷它是不是回文數。
即12321是回文數，個位與萬位相同，十位與千位相同。
```


#
```
給定一個字串（以字元陣列的形式）和一個偏移輛，根據偏移量原地從左向右旋轉字串。
輸入 str= "abcdefg", offset =3 ，輸出 “efgabcd" 。
輸入 str= "abcdefg", offset = 0，輸出“abcdefg" 。
。輸入str= "abcdefg",offset =2 ，輸出 “fgabcde" 
```



#
```
給定兩個字串 s和t，每次可以任意交換 s的奇數位或偶數位元上的字元，
即奇數位元上的字元能與其他奇數位元的字元互換，偶數位元上的字元也能與其他偶數位元的字元互換，
問能否經過若干次交換，使s 變成t。

輸入為 s= "abed",t = "cdab" ，輸出是 “Yes" ，第 1次a與c交換，第 2次b與d交換。
輸入 s= "abed",t = "bcda" ，輸出是 “No" ，無論如何交換，都無法得到 bcda。
```


#
```
給定一個陣列 a[]，其中除了 2個數，其他均出現 2次，請找到不重複的2 個數並返回。
給出 a= [1,2,5,5,6,6] ，返回 [1,2] ，除1和2 外其他數都出現了2 次，因此返回 [1,2] 
給出 a= [3,2,7,5,5,7] ，返回 [2,3] ，除了2和3 其他數都出現了2 次，因此返回 [2,3]
```

#
```
計算字串中的單詞數，其中一個單詞定義為不含空格的連續字串。
輸入 “Hello, my name is John" ，輸出5。
```

#
```
請撰寫一個age(n) 遞迴函數,計算某位人員的歲數
其中參數代表第幾位人士,

問題如下
有5個人坐在一起，問第五個人多少歲？他說比第4個人大5歲。
問第4個人歲數，他說比第3個人大5歲。
問第三個人，又說比第2人大5歲。
問第2個人，說比第一個人大5歲。
最後問第一個人，他說是20歲。

# 遞迴函數定義如下:
def age(n):
    遞迴程式
    return x 

# 輸入第幾位人士
age(4)
   
程式限制:請使用遞迴方法
```


#
```
問題如下
有5個人坐在一起，問第五個人多少歲？他說比第4個人大5歲。
問第4個人歲數，他說比第3個人大5歲。
問第三個人，又說比第2人大5歲。
問第2個人，說比第一個人大5歲。
最後問第一個人，他說是20歲。

# 遞迴函數定義如下:
def age(n):
    遞迴程式
    return x 

# 輸入第幾位人士
age(4)
   
程式限制:請使用遞迴方法
```


